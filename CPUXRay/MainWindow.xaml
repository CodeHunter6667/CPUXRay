<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CPUXRay.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CPUXRay"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:CPUXRay.ViewModel"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:models="using:CPUXRay.Models"
    mc:Ignorable="d"
    Title="CPUXRay">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <controls:TabView>
            <controls:TabViewItem Header="Geral">
                <ScrollViewer>
                    <StackPanel Padding="20">
                        <TextBlock Text="Resumo do Sistema" FontSize="24" Margin="0,0,0,20"/>
                        <TextBlock Text="CPU: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Cpu.Name, Mode=OneWay}"/>
                        <TextBlock Text="RAM Total: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Ram.TotalMemoryMB, Mode=OneWay}"/>
                        <TextBlock Text="GPU: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Gpu.Name, Mode=OneWay}"/>
                        <TextBlock Text="Armazenamento: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Storage[0].Model, Mode=OneWay}" />
                        <TextBlock Text="Placa-mãe: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Motherboard.Model, Mode=OneWay}"/>
                    </StackPanel>
                </ScrollViewer>
            </controls:TabViewItem>

            <controls:TabViewItem Header="CPU">
                <ScrollViewer>
                    <StackPanel Padding="20">
                        <TextBlock Text="Nome: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Cpu.Name, Mode=OneWay}" />
                        <TextBlock Text="Cores: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Cpu.CoreCount, Mode=OneWay}" />
                        <TextBlock Text="Clock Atual (GHz): " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Cpu.CurrentClockGhz, Mode=OneWay}" />
                        <TextBlock Text="Temperatura (°C): " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Cpu.TemperatureCelsius, Mode=OneWay}" />
                    </StackPanel>
                </ScrollViewer>
            </controls:TabViewItem>

            <controls:TabViewItem Header="RAM">
                <ScrollViewer>
                    <StackPanel Padding="20">
                        <TextBlock Text="Total (MB): " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Ram.TotalMemoryMB, Mode=OneWay}"/>
                        <TextBlock Text="Usado (MB): " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Ram.UsedMemomryMB, Mode=OneWay}"/>
                        <TextBlock Text="Disponível (MB): " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Ram.AvailableMemoryMB, Mode=OneWay}"/>
                        <TextBlock Text="Tipo: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Ram.MemoryType, Mode=OneWay}"/>
                    </StackPanel>
                </ScrollViewer>
            </controls:TabViewItem>

            <controls:TabViewItem Header="GPU">
                <ScrollViewer>
                    <StackPanel Padding="20">
                        <TextBlock Text="Nome: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Gpu.Name, Mode=OneWay}"/>
                        <TextBlock Text="Temperatura (°C): " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Gpu.TemperatureCelsius, Mode=OneWay}"/>
                        <TextBlock Text="Uso (%): " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Gpu.UsagePercentage, Mode=OneWay}"/>
                    </StackPanel>
                </ScrollViewer>
            </controls:TabViewItem>

            <controls:TabViewItem Header="Armazenamento">
                <ScrollViewer>
                    <StackPanel Padding="20">
                        <TextBlock Text="Discos Detectados" FontSize="18" Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{x:Bind ViewModel.Storage, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:StorageInfo">
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="{x:Bind Model}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind DriverLetter}"/>
                                        <TextBlock Text="{x:Bind TotalSpaceGB, Mode=OneWay, Converter={StaticResource GbFormatConverter}}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </controls:TabViewItem>

            <controls:TabViewItem Header="Placa-mãe">
                <ScrollViewer>
                    <StackPanel Padding="20">
                        <TextBlock Text="Modelo: " FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Motherboard.Model, Mode=OneWay}"/>
                    </StackPanel>
                </ScrollViewer>
            </controls:TabViewItem>

        </controls:TabView>
    </Grid>
</Window>
